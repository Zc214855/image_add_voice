# 代码导览构建

## 阶段 1：规范与输入确认

- 已读取 `tour-builder.md` 的完整构建规范。
- 已确认图数据来源为 `assembled-graph.json`，分层数据来源为 `layers.json`。
- 已确认项目总览文档、CLI 入口、GUI 入口与共享生成服务均存在于图节点中。
- 已创建拓扑分析脚本，后续导览只依据脚本输出设计。

## 阶段 2：拓扑分析

- 分析完成：59 个节点、112 条边。
- README 与 `main.py` 分别成为文档和代码首选入口。
- CLI 路径的 BFS 顺序为 `main.py`、`cli.py`、`media.py`/`service.py`、`alignment.py`/`subtitles.py`。
- GUI 入口、共享服务、高扇入函数、项目分层和非代码材料已纳入导览候选。

## 阶段 3：导览生成

- 已生成 11 个连续步骤。
- 导览覆盖 README、CLI/GUI、共享服务、故事素材、语音转写、原文对齐、ASS 字幕、FFmpeg 渲染、视觉规范和成片验收。
- 所有标题、描述和语言课程均使用中文。

## 阶段 4：结果校验

- JSON 语法有效。
- 步骤编号从 1 到 11 连续且无重复。
- 每步包含 1 至 5 个有效节点，未引用图外节点。
- 每段描述均为 2 至 4 句，导览数量符合 5 至 15 步约束。
